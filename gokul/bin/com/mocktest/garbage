Garbage collection (GC) is a memory recovery feature.It 
automatically free up memory space that has been allocated to objects no longer needed by the program and
memory space can then be used for future object allocations within that program.